describe('Test in demo.test.js file', () => {
  test('must be equals', () => {
    // inicializacion
    const m1='Kakaroto';
    // estimulo
    const m2="Kakaroto";
    // observar comportamiento
    expect(m1).toBe(m2);
  });
});

